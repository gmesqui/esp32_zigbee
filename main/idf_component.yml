## IDF Component Manager manifest
dependencies:
  idf: ">=5.3.0"
  espressif/esp-zigbee-lib:
    version: ">=1.6.0"
    rules:
      - if: "idf_version >= 5.3"
  espressif/esp-zboss-lib:
    version: ">=1.6.0"
    rules:
      - if: "idf_version >= 5.3"
  espressif/led_strip:
    version: ">=2.5.0"
  espressif/mdns:
    version: ">=1.2.0"
